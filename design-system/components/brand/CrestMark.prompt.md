Club wordmark lockup in Fraktur + motto. Pass `crest` (img src) to prepend the shield. `tone`: gold (on dark), navy (on light), mono.

```jsx
<CrestMark crest="assets/logo-shield.png" tone="gold" size="lg" />
```