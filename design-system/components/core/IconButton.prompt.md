Icon-only control for toolbars & nav. Always pass an accessible `label`.

```jsx
<IconButton label="Hinzufügen" variant="blau">＋</IconButton>
```

Variants: soft (default), blau, gold, ghost, outline. `size` is px square (default 40; keep ≥44 for primary mobile targets).