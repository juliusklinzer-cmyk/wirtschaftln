Member photo with initials fallback. `ring` = gold ring for office holders; `badge` = corner rank; `present` = green dot.

```jsx
<Avatar src={photo} name="Sepp Brunner" size={56} ring badge="1" />
```