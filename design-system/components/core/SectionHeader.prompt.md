Eyebrow + title row with optional trailing action. `fraktur` renders the title in blackletter — reserve for ceremonial/chronicle sections.

```jsx
<SectionHeader eyebrow="Saison 9" title="Der nächste Stammtisch" action={<Button size="sm">Alle</Button>} />
```