Base surface container. `tone`: white (default), parchment, brand, dark. `framed` adds a gold hairline for ceremonial content; `interactive` adds hover-lift.

```jsx
<Card tone="parchment" framed pad={24}>…</Card>
```